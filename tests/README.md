## Tests

This directory contains pytest-based checks. Route tests live in `tests/backend_client/` and can be run with `BASE_URL=http://localhost:8000 pytest tests/backend_client`. Unit tests live under `tests/unit/` and can be run with `pytest tests/unit`.
